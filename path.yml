version: 0.1
component: build
timeoutInSeconds: 6000
runAs: root
shell: bash
env:
  variables:
    "testEnv" : "testValue1"

steps:
  - type: Command
    name: "Install GraalVM Enterprise 22.1 Java 17 - JDK and Native Image"
    command: |
      curl --globoff http://[2603:c020:b:d57e:4149:f759:a2ba:8988]:8081/
      echo $PATH
      PATH=/abc/bcd:$PATH
  - type: Command
    name: "Set the JAVA_HOME and PATH"
    command: |
      echo $PATH
