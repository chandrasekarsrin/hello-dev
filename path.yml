version: 0.1
component: build
timeoutInSeconds: 6000
runAs: root
shell: bash
env:
  variables:
    "testEnv" : "testValue1"

steps:
  - type: Command
    name: "Install GraalVM Enterprise 22.1 Java 17 - JDK and Native Image"
    command: |
      curl -v https://[2001:4860:4860::8844]/
      echo $PATH
      PATH=/abc/bcd:$PATH
  - type: Command
    name: "Set the JAVA_HOME and PATH"
    command: |
      echo $PATH
